namespace Zuh.Compiler.Generation.Nodes {
    /// <summary>
    /// node that represents a scalar (you could also call it a leaf). since the only supported scalar type is string,
    /// they dont store any data for now.
    /// </summary>
    public class ScalarNode : INode;
}